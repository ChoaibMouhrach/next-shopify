import PublicLayout from "@/components/layouts/public";

export default function Page() {
  return (
    <PublicLayout className="flex justify-center">
      <div className="container flex items-center justify-center">
        <h1 className="text-6xl text-center font-extrabold tracking-wide">
          SHOPIFY STORE
        </h1>
      </div>
    </PublicLayout>
  );
}
